# Coordination Engine Configuration
# Copy this file to .env and customize for your environment

# ==============================================================================
# Server Configuration
# ==============================================================================

# HTTP server port (default: 8080)
PORT=8080

# Prometheus metrics port (default: 9090)
METRICS_PORT=9090

# Log level: debug, info, warn, error, fatal, panic (default: info)
LOG_LEVEL=info

# ==============================================================================
# Kubernetes Configuration
# ==============================================================================

# Path to kubeconfig file (optional, uses in-cluster config if not set)
# When running locally, set this to your kubeconfig path
# When running in-cluster, leave empty
KUBECONFIG=

# Namespace where the coordination engine is deployed (default: self-healing-platform)
NAMESPACE=self-healing-platform

# Kubernetes client QPS limit (queries per second) (default: 50.0)
KUBERNETES_QPS=50.0

# Kubernetes client burst limit (concurrent requests) (default: 100)
KUBERNETES_BURST=100

# ==============================================================================
# External Service URLs
# ==============================================================================

# Python ML/AI service endpoint for anomaly detection and predictions
# (default: http://aiops-ml-service:8080)
ML_SERVICE_URL=http://aiops-ml-service:8080

# ArgoCD API endpoint (optional, auto-detected from cluster if not set)
# Example: https://argocd-server.openshift-gitops.svc.cluster.local
ARGOCD_API_URL=

# ==============================================================================
# HTTP Client Configuration
# ==============================================================================

# HTTP client timeout for external service calls (default: 30s)
# Valid units: s (seconds), m (minutes), h (hours)
# Examples: 30s, 1m, 2m30s
HTTP_TIMEOUT=30s

# ==============================================================================
# CORS Configuration
# ==============================================================================

# Enable CORS for frontend applications (default: false)
ENABLE_CORS=false

# Allowed origins for CORS (comma-separated, default: *)
# Examples: http://localhost:3000,https://console.example.com
CORS_ALLOW_ORIGIN=*

# ==============================================================================
# Example Configurations for Different Environments
# ==============================================================================

# Development (local machine):
# KUBECONFIG=~/.kube/config
# ML_SERVICE_URL=http://localhost:8080
# LOG_LEVEL=debug
# ENABLE_CORS=true

# Staging (in-cluster):
# KUBECONFIG=
# ML_SERVICE_URL=http://aiops-ml-service.self-healing-platform.svc.cluster.local:8080
# LOG_LEVEL=info
# NAMESPACE=self-healing-platform

# Production (in-cluster):
# KUBECONFIG=
# ML_SERVICE_URL=http://aiops-ml-service.self-healing-platform.svc.cluster.local:8080
# LOG_LEVEL=warn
# NAMESPACE=self-healing-platform
# HTTP_TIMEOUT=60s
# KUBERNETES_QPS=100.0
# KUBERNETES_BURST=200
